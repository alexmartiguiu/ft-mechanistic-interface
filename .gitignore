# data + generated artifacts (vectors, responses, checkpoints) — keep dir skeleton
/data/**
!/data/**/
!/data/**/.gitkeep
*.npz
*.safetensors
adapters/
runs/
wandb/

# python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# secrets
.env

# Results NUMBERS (aggregate eval summaries + monitor trajectories + report) — keep these in
# git; the heavy raw per-question transcripts (*.jsonl), adapter weights (*.safetensors) and
# vectors (*.npz) stay ignored.
!/data/**/results/summary.json
!/data/**/results/**/*_summary.json
!/data/**/checkpoints/train_summary.json
!/data/**/posthoc_drift.json
!/data/report.html
