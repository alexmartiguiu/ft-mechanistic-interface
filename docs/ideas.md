# Ideas

Parking lot. Terse.

- **Concept-proposer agent.** Grow `propose_concepts()` into an agentic loop that drafts
  the concept set from the dataset + use-case and *maximises separability*: a tool scores
  inter-concept distance (description-embedding or fitted-vector cosine) and the agent
  searches for a maximally-distinct, non-redundant set. Flashy live demo.
